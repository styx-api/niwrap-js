// This file was auto generated by Styx.
// Do not edit this file directly.

import { Runner, Execution, Metadata, InputPathType, OutputPathType, getGlobalRunner } from 'styxdefs';

const ADJUNCT_IS_LABEL_PY_METADATA: Metadata = {
    id: "d2235f4fdb54ec6b5f9ee49cb86db0943973aad4.boutiques",
    name: "adjunct_is_label.py",
    package: "afni",
    container_image_tag: "afni/afni_make_build:AFNI_24.2.06",
};


interface AdjunctIsLabelPyParameters {
    "@type"?: "afni/adjunct_is_label.py";
    "infile": InputPathType;
    "label": string;
}
type AdjunctIsLabelPyParametersTagged = Required<Pick<AdjunctIsLabelPyParameters, '@type'>> & AdjunctIsLabelPyParameters;


/**
 * Output object returned when calling `AdjunctIsLabelPyParameters(...)`.
 *
 * @interface
 */
interface AdjunctIsLabelPyOutputs {
    /**
     * Output root folder. This is the root folder for all outputs.
     */
    root: OutputPathType;
}


/**
 * Build parameters.
 *
 * @param infile Input file for the adjunct_is_label script
 * @param label Output label generated by the script
 *
 * @returns Parameter dictionary
 */
function adjunct_is_label_py_params(
    infile: InputPathType,
    label: string,
): AdjunctIsLabelPyParametersTagged {
    const params = {
        "@type": "afni/adjunct_is_label.py" as const,
        "infile": infile,
        "label": label,
    };
    return params;
}


/**
 * Build command-line arguments from parameters.
 *
 * @param params The parameters.
 * @param execution The execution object for resolving input paths.
 *
 * @returns Command-line arguments.
 */
function adjunct_is_label_py_cargs(
    params: AdjunctIsLabelPyParameters,
    execution: Execution,
): string[] {
    const cargs: string[] = [];
    cargs.push("adjunct_is_label.py");
    cargs.push(execution.inputFile((params["infile"] ?? null)));
    cargs.push((params["label"] ?? null));
    return cargs;
}


/**
 * Build outputs object containing output file paths and possibly stdout/stderr.
 *
 * @param params The parameters.
 * @param execution The execution object for resolving input paths.
 *
 * @returns Outputs object.
 */
function adjunct_is_label_py_outputs(
    params: AdjunctIsLabelPyParameters,
    execution: Execution,
): AdjunctIsLabelPyOutputs {
    const ret: AdjunctIsLabelPyOutputs = {
        root: execution.outputFile("."),
    };
    return ret;
}


/**
 * adjunct_is_label.py
 *
 * A subsidiary script of the chauffeur_afni suite for label functionalities.
 *
 * Author: AFNI Developers
 *
 * URL: https://afni.nimh.nih.gov/
 *
 * @param params The parameters.
 * @param runner Command runner
 *
 * @returns NamedTuple of outputs (described in `AdjunctIsLabelPyOutputs`).
 */
function adjunct_is_label_py_execute(
    params: AdjunctIsLabelPyParameters,
    runner: Runner | null = null,
): AdjunctIsLabelPyOutputs {
    runner = runner || getGlobalRunner();
    const execution = runner.startExecution(ADJUNCT_IS_LABEL_PY_METADATA);
    params = execution.params(params)
    const cargs = adjunct_is_label_py_cargs(params, execution)
    const ret = adjunct_is_label_py_outputs(params, execution)
    execution.run(cargs, undefined);
    return ret;
}


/**
 * adjunct_is_label.py
 *
 * A subsidiary script of the chauffeur_afni suite for label functionalities.
 *
 * Author: AFNI Developers
 *
 * URL: https://afni.nimh.nih.gov/
 *
 * @param infile Input file for the adjunct_is_label script
 * @param label Output label generated by the script
 * @param runner Command runner
 *
 * @returns NamedTuple of outputs (described in `AdjunctIsLabelPyOutputs`).
 */
function adjunct_is_label_py(
    infile: InputPathType,
    label: string,
    runner: Runner | null = null,
): AdjunctIsLabelPyOutputs {
    const params = adjunct_is_label_py_params(infile, label)
    return adjunct_is_label_py_execute(params, runner);
}


export {
      ADJUNCT_IS_LABEL_PY_METADATA,
      AdjunctIsLabelPyOutputs,
      adjunct_is_label_py,
      adjunct_is_label_py_execute,
      adjunct_is_label_py_params,
};
